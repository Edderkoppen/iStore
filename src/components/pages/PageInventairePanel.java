package components.pages;

import javax.swing.*;
import java.awt.*;

public class PageInventairePanel {

    public static JPanel createInventoryPage() {
        JPanel pan = new JPanel();

        pan.setBackground(Color.MAGENTA);

        return pan;
    }
}
